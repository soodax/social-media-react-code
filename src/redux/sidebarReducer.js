let initialState = {
    friendsData: [
        { name: 'John' },
        { name: 'Nick' },
        { name: 'Bob' },
    ]
};

export const sidebarReducer = (state = initialState, action) => {
    return state;
}

export default sidebarReducer;
